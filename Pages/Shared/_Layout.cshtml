<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Empresa</title>

    <!-- 
      Inclusión de Bootstrap CSS desde CDN para estilos responsivos y componentes modernos.
      El atributo 'integrity' proporciona seguridad adicional.
    -->
    <link 
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" 
      rel="stylesheet" 
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN%" 
      crossorigin="anonymous" />

    <!-- 
      Inclusión de hoja de estilos personalizada para la aplicación.
      Permite sobreescribir o agregar estilos propios.
    -->
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header>
      <!-- 
        Barra de navegación principal utilizando componentes de Bootstrap.
        Incluye enlaces a las páginas principales de la aplicación.
      -->
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
          <!-- 
            Marca de la empresa, enlaza a la página de inicio.
          -->
          <a class="navbar-brand" asp-page="/Index">Empresa</a>
          <!-- 
            Botón para mostrar/ocultar el menú en dispositivos pequeños.
          -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav ms-auto">
              <li class="nav-item">
                <!-- Enlace a la página de inicio -->
                <a class="nav-link" asp-page="/Index">Inicio</a>
              </li>
              <li class="nav-item">
                <!-- Enlace a la página de cálculo de sueldo -->
                <a class="nav-link" asp-page="/CalcularSueldo">Calcular Sueldo</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <div class="container my-5">
      @RenderBody()
    </div>

    <footer class="text-center py-4">
      &copy; 2025 Empresa
    </footer>

    <!-- jQuery (necesario para validación) -->
    <script 
      src="https://code.jquery.com/jquery-3.6.0.min.js" 
      integrity="sha384-…hash…" 
      crossorigin="anonymous"></script>

    <!-- jQuery Validation -->
    <script 
      src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js" 
      integrity="sha384-…hash…" 
      crossorigin="anonymous"></script>
    <script 
      src="https://cdn.jsdelivr.net/npm/jquery-validation-unobtrusive@3.2.12/dist/jquery.validate.unobtrusive.min.js" 
      integrity="sha384-…hash…" 
      crossorigin="anonymous"></script>

    <!-- Bootstrap JS Bundle (Popper + Bootstrap) -->
    <script 
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" 
      integrity="sha384-…otro-hash…" 
      crossorigin="anonymous"></script>

    @RenderSection("Scripts", required: false)
</body>
</html>
